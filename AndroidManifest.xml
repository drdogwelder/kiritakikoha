<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="nz.net.catalyst.KiritakiKoha"
      android:versionCode="1"
      android:versionName="1.0"      >
    <application android:icon="@drawable/icon" android:label="@string/label" android:debuggable="false">
        <!-- 
        
        	Disabled auth, sync and login stuff since it's not working yet
        
	    <service android:name=".authenticator.AccountAuthenticatorService"
	    		android:exported="true">
	    	<intent-filter>
	    		<action android:name="android.accounts.AccountAuthenticator" />
	    	</intent-filter>
	    	<meta-data android:name="android.accounts.AccountAuthenticator"
	    			android:resource="@xml/koha_authenticator" />
	    </service>
	    
	    <service android:name=".syncadapter.SyncAdapterService"
	    		android:exported="true">
	    	<intent-filter>
	    		<action android:name="android.content.SyncAdapter" />
	    	</intent-filter>
	    	<meta-data android:name="android.content.SyncAdapter"
	    			android:resource="@xml/koha_syncadapter" />
	    </service>
	    
	    <activity android:name=".authenticator.LoginActivity"
	    	android:label="@string/login_activity_label"
	    	android:theme="@android:style/Theme.Dialog"
            android:excludeFromRecents="true" />
	-->    
    
        <activity android:name=".search.SearchActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
  	    <activity android:name=".search.ItemDetailsActivity" android:label="@string/itemdetails">
            <intent-filter>
                <action android:name="nz.net.catalyst.KiritakiKoha.ItemDetailsActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <activity android:name=".EditPreferences"
			android:label="@string/menu_preferences" />

    </application>
    <uses-sdk android:minSdkVersion="7" />
    <!-- permissions -->
    <!-- 
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission
        android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission
        android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission
        android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
         -->
    <uses-permission
        android:name="android.permission.INTERNET" />

</manifest> 